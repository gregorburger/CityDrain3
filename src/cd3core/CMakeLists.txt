file(GLOB_RECURSE CPP_FILES ./ *.cpp)

add_library(cd3core SHARED ${CPP_FILES})

TARGET_LINK_LIBRARIES(cd3core ${QT_LIBRARIES} ${Boost_SIGNALS_LIBRARY})

#INSTALL(TARGETS cd3core
#  LIBRARY DESTINATION lib
#)
